#include "TippeingabeDialog.h"

TippeingabeDialog::TippeingabeDialog(wxWindow* parent,
        wxWindowID id,
        const wxString& title)
    : wxDialog(parent, id, title,
            wxDefaultPosition, wxSize(400, 200))
{
    //ctor
}

TippeingabeDialog::~TippeingabeDialog()
{
    //dtor
}
